import React from 'react';
import {SafeAreaView, ScrollView, Text} from 'react-native';

const Home: React.FC = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Home Page</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Home;
